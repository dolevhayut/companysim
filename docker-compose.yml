services:
  companysim:
    build:
      context: .
      dockerfile: docker/Dockerfile
    ports:
      - "127.0.0.1:4545:4545"
    volumes:
      - company-data:/data
    environment:
      - OPENAI_API_KEY
      - ANTHROPIC_API_KEY
    restart: unless-stopped
volumes:
  company-data:
